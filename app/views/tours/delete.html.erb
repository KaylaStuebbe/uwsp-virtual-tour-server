<% provide(:title, "Delete " + @tour.name + "?") %>
<h1>Your're  about to delete the <%= @tour.name %> tour!</h1>

<p>This is probably bad, you can't undo this!</p>

<p>Do you want to delete this tour and all of it's points of interest?</p>

<%= link_to "Actually... No, I don't", @tour, :class => 'btn' %>
<%= link_to 'Yeah, delete it', @tour, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger' %>